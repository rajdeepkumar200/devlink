# DevLink REST API Documentation

Welcome to the DevLink REST API reference documentation. DevLink provides a robust set of RESTful APIs designed for developer profiles, project creation, team application management, social feeds, direct messaging, search, AI recommendations, and repository quality metrics.

---

## 📌 Interactive API Documentation (Swagger & ReDoc)

When running the DevLink backend server locally (`uvicorn app.main:app --reload` at `http://localhost:8000`), interactive API documentation is automatically generated by FastAPI:

* **Swagger UI (Interactive API Explorer)**: [http://localhost:8000/docs](http://localhost:8000/docs)
* **ReDoc (Detailed OpenAPI Reference)**: [http://localhost:8000/redoc](http://localhost:8000/redoc)
* **OpenAPI Specification (JSON Schema)**: [http://localhost:8000/openapi.json](http://localhost:8000/openapi.json)

---

## 🔐 Authentication & Headers

### Authentication Scheme
DevLink uses standard **Bearer Token (JWT)** authentication.
Pass the access token received during login or registration in the HTTP `Authorization` header:

```http
Authorization: Bearer <your_jwt_access_token>
```

### Base URL & API Versioning
* **Version 1 Base URL (Recommended)**: `http://localhost:8000/api/v1` (Production: `https://api.devlink.app/api/v1`)
* **Legacy Base URL (Backward Compatible)**: `http://localhost:8000/api` (Production: `https://api.devlink.app/api`)

> [!NOTE]
> DevLink supports explicit URL Path Versioning (`/api/v1`). Legacy unversioned `/api/` endpoints are maintained for backward compatibility.
> For detailed versioning rules, breaking change policies, and migration instructions, see the [API Versioning Strategy & Migration Guide](file:///Users/nayanraj/devlink/docs/api_versioning_and_migration.md).

### Common HTTP Headers
| Header | Type | Description |
| :--- | :--- | :--- |
| `Content-Type` | `string` | Must be `application/json` for POST, PUT, and PATCH endpoints |
| `Authorization` | `string` | `Bearer <access_token>` for authenticated endpoints |
| `X-Request-ID` | `string` | Optional unique correlation ID for tracking requests |

---

## ⚠️ Standard Error Responses

All error responses across the API follow a unified JSON payload structure:

```json
{
  "success": false,
  "message": "Error description message",
  "detail": [
    {
      "loc": ["body", "email"],
      "msg": "field required",
      "type": "value_error.missing"
    }
  ]
}
```

### Common HTTP Status Codes
* `200 OK`: Request succeeded.
* `201 Created`: Resource successfully created.
* `204 No Content`: Resource deleted or action completed with no response body.
* `400 Bad Request`: Invalid parameters or business rule violation.
* `401 Unauthorized`: Missing, invalid, or expired JWT token.
* `403 Forbidden`: Authenticated user lacks permission for the resource.
* `404 Not Found`: Requested resource does not exist.
* `422 Unprocessable Entity`: Request body failed Pydantic validation.
* `429 Too Many Requests`: Rate limit threshold exceeded.
* `500 Internal Server Error`: Unexpected server issue.

---

## 🔑 Authentication Endpoints (`/api/auth`)

### 1. Register User
Creates a new developer account on DevLink.

* **Endpoint**: `/api/auth/register`
* **Method**: `POST`
* **Authentication**: None (Public)
* **Request Body**:
```json
{
  "email": "developer@example.com",
  "username": "dev_builder",
  "password": "SecurePassword123!",
  "full_name": "Alex Rivera",
  "bio": "Full-stack developer passionate about open source and AI."
}
```
* **Response (`201 Created`)**:
```json
{
  "success": true,
  "message": "User registered successfully",
  "data": {
    "user": {
      "id": "c1f2e3d4-5678-90ab-cdef-1234567890ab",
      "email": "developer@example.com",
      "username": "dev_builder",
      "full_name": "Alex Rivera",
      "created_at": "2026-07-27T10:00:00Z"
    },
    "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "token_type": "bearer"
  }
}
```
* **Error Responses**:
  * `400 Bad Request`: Username or Email already exists.
  * `422 Unprocessable Entity`: Weak password or invalid email format.

---

### 2. User Login
Authenticates an existing user and returns JWT access and refresh tokens.

* **Endpoint**: `/api/auth/login`
* **Method**: `POST`
* **Authentication**: None (Public)
* **Request Body**:
```json
{
  "email": "developer@example.com",
  "password": "SecurePassword123!"
}
```
* **Response (`200 OK`)**:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh_token": "d9876543-21ba-fedc-ba09-876543210fed",
  "token_type": "bearer",
  "expires_in": 86400
}
```
* **Error Responses**:
  * `401 Unauthorized`: Invalid credentials.

---

### 3. Refresh Access Token
Generates a new access token using a valid refresh token.

* **Endpoint**: `/api/auth/refresh`
* **Method**: `POST`
* **Authentication**: None (Public)
* **Request Body**:
```json
{
  "refresh_token": "d9876543-21ba-fedc-ba09-876543210fed"
}
```
* **Response (`200 OK`)**:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer",
  "expires_in": 86400
}
```

---

### 4. GitHub OAuth Authentication Link
Returns the authorization URL for GitHub OAuth login flow.

* **Endpoint**: `/api/auth/github`
* **Method**: `GET`
* **Authentication**: None (Public)
* **Response (`200 OK`)**:
```json
{
  "url": "https://github.com/login/oauth/authorize?client_id=123456789&scope=user,repo"
}
```

---

### 5. GitHub OAuth Callback
Handles authorization code exchange with GitHub and authenticates the user.

* **Endpoint**: `/api/auth/github/callback`
* **Method**: `GET`
* **Authentication**: None (Public)
* **Query Parameters**:
  * `code` (`string`, required): OAuth authorization code from GitHub.
* **Response (`200 OK`)**:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer",
  "user": {
    "id": "c1f2e3d4-5678-90ab-cdef-1234567890ab",
    "github_username": "alexrivera",
    "email": "alex@github.com"
  }
}
```

---

## 👤 User Profile Endpoints (`/api/users`)

### 1. Get Current User Profile
Retrieves detailed profile information for the authenticated user.

* **Endpoint**: `/api/users/me`
* **Method**: `GET`
* **Authentication**: Bearer Token (Required)
* **Response (`200 OK`)**:
```json
{
  "id": "c1f2e3d4-5678-90ab-cdef-1234567890ab",
  "username": "dev_builder",
  "email": "developer@example.com",
  "full_name": "Alex Rivera",
  "headline": "Senior Full Stack Engineer",
  "bio": "Building scalable React & FastAPI web applications.",
  "avatar_url": "https://cdn.devlink.app/avatars/dev_builder.png",
  "github_username": "alexrivera",
  "skills": ["React", "TypeScript", "FastAPI", "PostgreSQL", "Docker"],
  "social_links": {
    "github": "https://github.com/alexrivera",
    "twitter": "https://twitter.com/alexrivera",
    "linkedin": "https://linkedin.com/in/alexrivera"
  },
  "created_at": "2026-07-27T10:00:00Z"
}
```

---

### 2. Update Current User Profile
Updates profile bio, skills, headline, or social links.

* **Endpoint**: `/api/users/me`
* **Method**: `PUT`
* **Authentication**: Bearer Token (Required)
* **Request Body**:
```json
{
  "headline": "Lead Platform Engineer @ DevLink",
  "bio": "Specializing in cloud systems, microservices, and AI integrations.",
  "skills": ["Python", "FastAPI", "PostgreSQL", "Redis", "Kubernetes"],
  "social_links": {
    "github": "https://github.com/alexrivera",
    "website": "https://alexrivera.dev"
  }
}
```
* **Response (`200 OK`)**:
```json
{
  "success": true,
  "message": "Profile updated successfully",
  "data": {
    "id": "c1f2e3d4-5678-90ab-cdef-1234567890ab",
    "username": "dev_builder",
    "headline": "Lead Platform Engineer @ DevLink"
  }
}
```

---

### 3. List & Search Users
Browse developer profiles with optional filtering by search query or skill tags.

* **Endpoint**: `/api/users`
* **Method**: `GET`
* **Authentication**: None (Public)
* **Query Parameters**:
  * `query` (`string`, optional): Search term matching username, name, or bio.
  * `skill` (`string`, optional): Filter by specific skill.
  * `limit` (`int`, optional, default: 20): Results per page.
  * `offset` (`int`, optional, default: 0): Pagination offset.
* **Response (`200 OK`)**:
```json
{
  "total": 42,
  "users": [
    {
      "id": "c1f2e3d4-5678-90ab-cdef-1234567890ab",
      "username": "dev_builder",
      "full_name": "Alex Rivera",
      "headline": "Senior Full Stack Engineer",
      "skills": ["React", "FastAPI"]
    }
  ]
}
```

---

### 4. Export User Data
Export all user profile data, applications, and created projects in JSON format.

* **Endpoint**: `/api/users/me/export`
* **Method**: `GET`
* **Authentication**: Bearer Token (Required)
* **Response (`200 OK`)**:
```json
{
  "exported_at": "2026-07-27T10:30:00Z",
  "user_profile": { ... },
  "projects": [ ... ],
  "applications": [ ... ]
}
```

---

## 🚀 Projects Endpoints (`/api/projects`)

### 1. List Projects
Browse open-source projects and team recruitment postings.

* **Endpoint**: `/api/projects`
* **Method**: `GET`
* **Authentication**: None (Public)
* **Query Parameters**:
  * `tag` (`string`, optional): Filter by project tag/category.
  * `tech_stack` (`string`, optional): Filter by technology.
  * `search` (`string`, optional): Keyword search in title/description.
  * `limit` (`int`, default: 20): Page size.
* **Response (`200 OK`)**:
```json
{
  "total": 15,
  "projects": [
    {
      "id": "p9876543-21ba-fedc-ba09-876543210fed",
      "title": "DevLink Open Source Collaboration Platform",
      "tagline": "Connect with builders and ship projects together.",
      "category": "Web App",
      "tech_stack": ["React", "FastAPI", "PostgreSQL"],
      "owner_id": "c1f2e3d4-5678-90ab-cdef-1234567890ab",
      "stars_count": 128,
      "created_at": "2026-07-20T12:00:00Z"
    }
  ]
}
```

---

### 2. Create Project
Post a new project for collaboration or team member recruitment.

* **Endpoint**: `/api/projects`
* **Method**: `POST`
* **Authentication**: Bearer Token (Required)
* **Request Body**:
```json
{
  "title": "AI Code Assistant",
  "tagline": "Automated code analysis & suggestion CLI",
  "description": "Building an open source LLM-powered assistant for developers.",
  "category": "AI/ML",
  "tech_stack": ["Python", "OpenAI", "Click"],
  "repository_url": "https://github.com/example/ai-code-assistant",
  "open_roles": ["Frontend Dev", "AI Engineer"]
}
```
* **Response (`201 Created`)**:
```json
{
  "success": true,
  "id": "p1122334-5566-7788-9900-aabbccddeeff",
  "title": "AI Code Assistant",
  "created_at": "2026-07-27T10:45:00Z"
}
```

---

### 3. Get Project Details
Retrieve comprehensive information for a specific project.

* **Endpoint**: `/api/projects/{project_id}`
* **Method**: `GET`
* **Authentication**: None (Public)
* **Response (`200 OK`)**:
```json
{
  "id": "p9876543-21ba-fedc-ba09-876543210fed",
  "title": "DevLink Open Source Collaboration Platform",
  "description": "Comprehensive platform details...",
  "tech_stack": ["React", "FastAPI", "PostgreSQL"],
  "open_roles": ["Backend Developer", "UI Designer"],
  "owner": {
    "id": "c1f2e3d4-5678-90ab-cdef-1234567890ab",
    "username": "dev_builder"
  }
}
```

---

## 📝 Applications Endpoints (`/api/applications`)

### 1. Submit Application to Join Project
Submit an application to contribute to a specific project.

* **Endpoint**: `/api/applications`
* **Method**: `POST`
* **Authentication**: Bearer Token (Required)
* **Request Body**:
```json
{
  "project_id": "p9876543-21ba-fedc-ba09-876543210fed",
  "role_applied": "Backend Developer",
  "cover_note": "I have 3 years of experience with FastAPI and PostgreSQL."
}
```
* **Response (`201 Created`)**:
```json
{
  "id": "app_9988776655",
  "status": "pending",
  "created_at": "2026-07-27T11:00:00Z"
}
```

---

### 2. Update Application Status
Project owners accept or reject applications.

* **Endpoint**: `/api/applications/{application_id}/status`
* **Method**: `PATCH`
* **Authentication**: Bearer Token (Required - Project Owner)
* **Request Body**:
```json
{
  "status": "accepted"
}
```
* **Response (`200 OK`)**:
```json
{
  "success": true,
  "message": "Application status updated to accepted"
}
```

---

## 💬 Real-Time Messaging & Conversations (`/api/messages` & `/api/conversations`)

### 1. List User Conversations
Get direct message conversations active for the authenticated user.

* **Endpoint**: `/api/conversations`
* **Method**: `GET`
* **Authentication**: Bearer Token (Required)
* **Response (`200 OK`)**:
```json
[
  {
    "id": "conv_12345",
    "participant": {
      "id": "user_789",
      "username": "sarah_designer"
    },
    "last_message": "Sounds great! Let's start the design review tomorrow.",
    "updated_at": "2026-07-27T10:15:00Z"
  }
]
```

---

### 2. Send Direct Message
Send a text message in a conversation.

* **Endpoint**: `/api/messages`
* **Method**: `POST`
* **Authentication**: Bearer Token (Required)
* **Request Body**:
```json
{
  "conversation_id": "conv_12345",
  "content": "Hey Sarah, check out the updated API documentation!"
}
```
* **Response (`201 Created`)**:
```json
{
  "id": "msg_554433",
  "conversation_id": "conv_12345",
  "sender_id": "c1f2e3d4-5678-90ab-cdef-1234567890ab",
  "content": "Hey Sarah, check out the updated API documentation!",
  "created_at": "2026-07-27T11:05:00Z"
}
```

---

## 🔍 Search & AI Endpoints (`/api/search`, `/api/recommendations`, `/api/repository-quality`)

### 1. Global Search
Search across users, projects, and issues.

* **Endpoint**: `/api/search`
* **Method**: `GET`
* **Authentication**: None (Public)
* **Query Parameters**:
  * `q` (`string`, required): Search string query.
* **Response (`200 OK`)**:
```json
{
  "users": [ ... ],
  "projects": [ ... ],
  "issues": [ ... ]
}
```

---

### 2. Teammate Recommendations (AI Engine)
Returns intelligent project and collaborator recommendations for the logged-in user.

* **Endpoint**: `/api/recommendations/projects`
* **Method**: `GET`
* **Authentication**: Bearer Token (Required)
* **Response (`200 OK`)**:
```json
{
  "recommendations": [
    {
      "project_id": "p9876543-21ba-fedc-ba09-876543210fed",
      "title": "DevLink Platform",
      "match_score": 0.94,
      "reasons": ["Matching skills: FastAPI, TypeScript", "Open Backend Developer role"]
    }
  ]
}
```

---

### 3. Repository Quality Score
Calculates quality score metrics for a connected GitHub repository.

* **Endpoint**: `/api/repository-quality/{repo_id}`
* **Method**: `GET`
* **Authentication**: Bearer Token (Required)
* **Response (`200 OK`)**:
```json
{
  "repo_id": "repo_123",
  "quality_score": 88,
  "metrics": {
    "documentation": "High",
    "test_coverage": "78%",
    "code_activity": "Active"
  }
}
```

---

## 🏥 Health & System Status (`/health`)

### 1. System Health Status
Simple health status endpoint for reverse proxy, load balancer, and container orchestration probes.

* **Endpoint**: `/health`
* **Method**: `GET`
* **Authentication**: None (Public)
* **Response (`200 OK`)**:
```json
{
  "status": "healthy",
  "environment": "development"
}
```

---

### 2. Backend Health Check Dashboard
Detailed health status dashboard endpoint reporting service status for Database, Redis, AI Service, Storage, and System Uptime.

* **Endpoint**: `/health/dashboard` (or `/api/v1/health/dashboard`)
* **Method**: `GET`
* **Authentication**: None (Public)
* **Query Parameters**:
  * `format` (`string`, optional): Set to `html` to view an interactive visual dashboard in the browser.
* **Response (`200 OK`)**:
```json
{
  "status": "healthy",
  "timestamp": "2026-07-28T22:34:25Z",
  "environment": "development",
  "uptime": {
    "seconds": 1234.56,
    "human": "20 minutes, 34 seconds",
    "started_at": "2026-07-28T22:13:51Z"
  },
  "services": {
    "database": {
      "status": "healthy",
      "latency_ms": 1.25
    },
    "redis": {
      "status": "healthy",
      "latency_ms": 0.85
    },
    "ai_service": {
      "status": "healthy",
      "provider": "OpenAI",
      "configured": true,
      "latency_ms": 12.4
    },
    "storage": {
      "status": "healthy",
      "path": "uploads",
      "writable": true,
      "free_space_mb": 45120.5,
      "total_space_mb": 500000.0,
      "used_space_mb": 454879.5
    },
    "celery": {
      "status": "healthy",
      "workers": 2
    }
  }
}
```
